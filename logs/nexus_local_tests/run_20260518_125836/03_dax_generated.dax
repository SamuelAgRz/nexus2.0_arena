EVALUATE
SUMMARIZECOLUMNS(
    'Channel'[Trade Channel],
    TREATAS({"Colombia"}, 'Ship From'[Country]),
    TREATAS({"Actual"}, 'Record Type'[Record Type]),
    TREATAS({"2025"}, 'Period'[Year 445]),
    "Unit Cases", [Unit Cases AC]
)
ORDER BY [Unit Cases] DESC